# ExpenseTracker API test configuration
# Values here are used when running tests against a pre-started app instance.
# When AppFixture manages the process (default), these are overridden at runtime.

base.url=http://localhost:5001
app.port=5001

# List endpoint path — update if implementation uses /expenses instead of /
list.path=/
